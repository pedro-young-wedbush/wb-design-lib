import styled from 'styled-components'

const StyledSidePane = styled.div`
  display: flex;
  flex: 1;
  /* min-height: 100vh; */
  color: black;
  background: #ffffff;
`

export default StyledSidePane
